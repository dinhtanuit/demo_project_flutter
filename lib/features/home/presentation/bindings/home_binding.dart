import 'package:get/get.dart';

class HomeBinding extends Bindings {
  @override
  void dependencies() {
    // Home không cần controller phức tạp
    // Thêm dependencies ở đây nếu cần sau này
  }
}
